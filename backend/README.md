# SyncCode Backend

This folder contains the backend service skeleton. Populate with NestJS or Fastify implementation.

Commands:
- `pnpm install` or `npm install`
- `pnpm run dev` to start dev server (configure as needed)
